'use client'

import React from 'react'

export const GamingBackground: React.FC = () => {
  // Return null to disable all canvas grid loops and glow meshes for a clean monochrome interface
  return null
}
